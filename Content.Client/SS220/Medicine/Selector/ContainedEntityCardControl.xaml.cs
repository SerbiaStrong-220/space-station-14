
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface;
using Robust.Client.UserInterface.XAML;

namespace Content.Client.SS220.Medicine.Selector;

[GenerateTypedNameReferences]
public sealed partial class ContainedEntityCardControl : Control
{
    public Action? OnPressed;
    public ContainedEntityCardControl(string label)
    {
        RobustXamlLoader.Load(this);

        NameLabel.SetMessage(label);

        MainButton.OnPressed += _ => OnPressed?.Invoke();
    }
}