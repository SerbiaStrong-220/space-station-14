<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      xmlns:x="http://schemas.microsoft.com">
    <BoxContainer Orientation="Horizontal" MinSize="550 450">
        <BoxContainer Orientation="Vertical" MinWidth="160" Margin="5">
            <Label Text="{Loc 'ipc-face-customization-cat'}" HorizontalAlignment="Center" Margin="0 0 0 5" />
            <ScrollContainer VerticalExpand="True">
                <BoxContainer Name="CategoryList" Orientation="Vertical" VerticalExpand="True" />
            </ScrollContainer>
        </BoxContainer>
        <PanelContainer MinWidth="2" Margin="4 0" VerticalExpand="True">
        </PanelContainer>
        <BoxContainer Orientation="Vertical" HorizontalExpand="True" Margin="5">
            <Label Text="{Loc 'ipc-face-customization-screen'}" HorizontalAlignment="Center" Margin="0 0 0 5" />
            <ScrollContainer VerticalExpand="True">
                <BoxContainer Name="OptionsContainer" Orientation="Vertical" HorizontalExpand="True" />
            </ScrollContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
